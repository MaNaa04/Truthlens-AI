# Environment configuration
# Copy this file to .env and fill in your API keys

# Application Settings
APP_DEBUG=false

# Server Configuration
HOST=0.0.0.0
PORT=8000

# LLM Configuration
# Provider: "gemini" (free tier) or "openai"
LLM_PROVIDER=gemini
LLM_API_KEY=your_llm_api_key_here
LLM_MODEL=gemini-2.0-flash
# For OpenAI: LLM_PROVIDER=openai, LLM_MODEL=gpt-4, LLM_API_BASE=https://api.openai.com/v1

# External APIs
SERPAPI_KEY=your_serpapi_key_here
WIKIPEDIA_API_ENABLED=true

# Caching
CACHE_ENABLED=true
CACHE_TTL_SECONDS=3600

# Redis  (use redis://redis:6379/0 inside Docker Compose)
REDIS_URL=redis://localhost:6379/0
REDIS_ENABLED=true

# CORS — set to your Chrome Extension origin when Dev 1 ships JWT auth
# Example: ALLOWED_ORIGINS=chrome-extension://abcdefghijklmnopabcdefghijklmnop
ALLOWED_ORIGINS=["*"]

# Evidence Configuration
MAX_EVIDENCE_TOKENS=800
MAX_CLAIMS_PER_REQUEST=3

# JWT Authentication
# Copy from your Supabase / Firebase project settings dashboard.
# HS256 (default): paste the JWT secret string.
# RS256:           paste the provider's PEM-encoded public key and set JWT_ALGORITHM=RS256.
JWT_SECRET=your_jwt_secret_here
JWT_ALGORITHM=HS256
JWT_EXPIRY_SECONDS=3600

# MongoDB (per-user history — separate from Redis global cache)
# Local dev: mongodb://localhost:27017
# Atlas:     mongodb+srv://<user>:<pass>@cluster.mongodb.net
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=aimatrix_db
