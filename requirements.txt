fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.10.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Caching
cachetools>=5.3.0
redis[asyncio]>=5.0.0

# External APIs
wikipedia-api>=0.12.0
google-search-results>=2.4.0

# LLM Clients
openai>=1.3.0
anthropic>=0.39.0

# Authentication (JWT verification)
# python-jose supports HS256 (symmetric) and RS256/ES256 (asymmetric) out of the box.
python-jose[cryptography]>=3.3.0

# Rate limiting (user-scoped, not IP-based)
slowapi>=0.1.9

# Database (per-user history)
motor>=3.3.0           # Async MongoDB driver built on top of PyMongo
pymongo>=4.6.0         # Required by motor; also provides IndexModel helpers

# Development / Testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
httpx>=0.25.2
